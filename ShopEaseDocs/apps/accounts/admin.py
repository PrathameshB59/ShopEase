"""
Admin configuration for accounts app.

This app uses Django's default auth.User from ShopEase database.
No custom admin configuration needed.
"""
from django.contrib import admin

# No models to register - using Django's default auth.User from ShopEase
